{
  "name": "current-portfolio-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo root — Husky hooks live here",
  "scripts": {
    "prepare": "husky",
    "lint:frontend": "cd Frontend-portfolio && npm run lint",
    "build:frontend": "cd Frontend-portfolio && npm run build",
    "install:all": "npm ci && cd Frontend-portfolio && npm ci && cd ../Backend-portfolio && npm ci"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
